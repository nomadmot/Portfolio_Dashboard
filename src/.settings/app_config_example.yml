defaults:
  # default settings for daily performance summary
  performance_summary_period: D50 # default period for the time machine
  performance_summary_symbol: QQQ # default comparison symbol

# set the log mode for the specified modules to debug
# NOTE: Streamlit pages run under the name "__main__"
# debug: [
#   utility.time_machine_component,
#   __main__
#   ]
